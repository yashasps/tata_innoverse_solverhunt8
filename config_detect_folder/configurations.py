MODEL_PATH = "yolo-coco" #path to model

# initializes minimum probabilities to filter weak detections along with the threshold when applying non-maxima suppression
MIN_CONF = 0.3
NMS_THRESH = 0.3

# define the minimum safe distance (in pixels) that two people can befrom each other
MIN_DISTANCE = 120